import { BookOpen, Sparkles } from "lucide-react";
import { requireEntitlement } from "@/lib/access";
import { loadDevotionals } from "@/lib/content";
import { requireUser } from "@/lib/session";

export default async function DevotionalsPage(){const user=await requireUser();await requireEntitlement(user);return <div className="space-y-7"><header><p className="text-sm font-semibold uppercase tracking-[.16em] text-[#B85C42]">Devocionales</p><h1 className="editorial-title mt-3 text-4xl sm:text-6xl">Treinta pausas para cuidar tu forma de amar.</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-muted">Una referencia bíblica, una reflexión, una oración y una acción. Sin negar límites ni sustituir ayuda profesional.</p></header><div className="grid gap-4 md:grid-cols-2">{loadDevotionals().map(item=><details key={item.day} className="app-card group p-5 open:shadow-lg"><summary className="flex cursor-pointer list-none items-center gap-4"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-[#e4e9df] font-semibold text-[#547055]">{item.day}</span><div className="flex-1"><p className="text-xs font-semibold uppercase tracking-wider text-muted">{item.reading}</p><h2 className="mt-1 font-semibold">{item.title}</h2></div><BookOpen size={18} className="text-[#B85C42]"/></summary><div className="mt-5 border-t border-[#ddd3c6] pt-5"><p className="leading-7 text-muted">{item.reflection}</p><div className="mt-4 rounded-xl bg-[#282621] p-4 text-[#f9f3e9]"><p className="text-xs uppercase tracking-wider text-[#d9a491]">Oración</p><p className="mt-2 leading-7">{item.prayer}</p></div><p className="mt-4 flex gap-2 leading-7"><Sparkles size={17} className="mt-1 shrink-0 text-[#B99255]"/>{item.action}</p></div></details>)}</div></div>}
