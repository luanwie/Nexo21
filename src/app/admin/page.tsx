import { BookOpen, DollarSign, KeyRound, Package, Power, Users } from "lucide-react";
import { grantAccess, revokeAccess, toggleProduct } from "@/app/admin/actions";
import { loadActions, loadConversations, loadDevotionals, loadJourney, loadMessages } from "@/lib/content";
import { prisma } from "@/lib/prisma";

export default async function AdminPage(){const [users,products,purchases,entitlements]=await Promise.all([prisma.user.findMany({orderBy:{createdAt:"desc"},take:20}),prisma.product.findMany({orderBy:{sortOrder:"asc"}}),prisma.purchase.findMany({include:{items:{include:{product:true}}},orderBy:{createdAt:"desc"},take:20}),prisma.entitlement.findMany({where:{status:"ACTIVE"},include:{user:true,product:true},orderBy:{grantedAt:"desc"},take:30})]);const revenue=purchases.filter(item=>item.status==="PAID").reduce((sum,item)=>sum+item.amountCents,0);return <div className="space-y-8"><header><p className="text-sm font-semibold uppercase tracking-[.16em] text-[#B85C42]">Administración</p><h1 className="editorial-title mt-3 text-4xl sm:text-6xl">Operación de Nexo 21.</h1><p className="mt-3 text-muted">Usuarios, ofertas, pedidos, contenido y desbloqueos.</p></header><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[{icon:Users,label:"Usuarios",value:users.length},{icon:Package,label:"Productos",value:products.length},{icon:DollarSign,label:"Pagos registrados",value:`US$${(revenue/100).toFixed(2)}`},{icon:BookOpen,label:"Piezas de contenido",value:loadJourney().length+loadMessages().length+loadConversations().length+loadActions().length+loadDevotionals().length}].map(({icon:Icon,label,value})=><div key={label} className="app-card p-5"><Icon className="text-[#B85C42]"/><p className="mt-4 text-2xl font-semibold">{value}</p><p className="text-sm text-muted">{label}</p></div>)}</section><section className="grid gap-5 lg:grid-cols-2"><div className="app-card p-6"><h2 className="flex items-center gap-2 font-semibold"><KeyRound size={18}/> Desbloqueo manual</h2><form action={grantAccess} className="mt-4 space-y-3"><input className="field" name="email" type="email" placeholder="email de la usuaria" required/><select className="field" name="productSlug" required>{products.map(product=><option key={product.id} value={product.slug}>{product.title}</option>)}</select><button className="primary-button w-fit">Conceder acceso</button></form></div><div className="app-card p-6"><h2 className="font-semibold">Inventario editorial</h2><div className="mt-4 grid grid-cols-2 gap-3 text-sm">{[["Jornada",loadJourney().length],["Mensajes",loadMessages().length],["Conversaciones",loadConversations().length],["Acciones",loadActions().length],["Devocionales",loadDevotionals().length]].map(([label,value])=><div key={String(label)} className="rounded-xl bg-[#f4eee4] p-3"><strong className="block text-xl">{value}</strong><span className="text-muted">{label}</span></div>)}</div></div></section><section className="app-card overflow-hidden"><div className="border-b border-[#ddd3c6] p-5"><h2 className="font-semibold">Productos</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-[#f4eee4] text-muted"><tr><th className="p-3">Producto</th><th>Tipo</th><th>Precio</th><th>Estado</th><th></th></tr></thead><tbody>{products.map(product=><tr key={product.id} className="border-t border-[#eee6db]"><td className="p-3 font-medium">{product.title}</td><td>{product.type}</td><td>US${(product.priceCents/100).toFixed(2)}</td><td>{product.active?"Activo":"Inactivo"}</td><td><form action={toggleProduct}><input type="hidden" name="id" value={product.id}/><input type="hidden" name="active" value={String(product.active)}/><button className="secondary-button text-xs"><Power size={14}/>{product.active?"Desactivar":"Activar"}</button></form></td></tr>)}</tbody></table></div></section><section className="grid gap-5 xl:grid-cols-2"><div className="app-card overflow-hidden"><div className="p-5"><h2 className="font-semibold">Últimos usuarios</h2></div><div className="divide-y divide-[#eee6db]">{users.map(user=><div key={user.id} className="flex justify-between gap-3 px-5 py-3 text-sm"><div><p className="font-medium">{user.name}</p><p className="text-muted">{user.email}</p></div><span className="pill bg-[#f4eee4] text-muted">{user.role}</span></div>)}</div></div><div className="app-card overflow-hidden"><div className="p-5"><h2 className="font-semibold">Últimos pedidos</h2></div><div className="divide-y divide-[#eee6db]">{purchases.map(purchase=><div key={purchase.id} className="px-5 py-3 text-sm"><div className="flex justify-between gap-3"><p className="font-medium">{purchase.purchaserEmail}</p><span className="pill bg-[#f4eee4] text-muted">{purchase.status}</span></div><p className="mt-1 text-muted">{purchase.items.map(item=>item.product.title).join(", ")} · US${(purchase.amountCents/100).toFixed(2)}</p></div>)}</div></div></section><section className="app-card overflow-hidden"><div className="p-5"><h2 className="font-semibold">Accesos activos</h2></div><div className="divide-y divide-[#eee6db]">{entitlements.map(item=><div key={item.id} className="flex flex-col gap-3 px-5 py-3 text-sm sm:flex-row sm:items-center sm:justify-between"><div><p className="font-medium">{item.user.email}</p><p className="text-muted">{item.product.title}</p></div><form action={revokeAccess}><input type="hidden" name="entitlementId" value={item.id}/><button className="secondary-button text-xs">Revocar</button></form></div>)}</div></section></div>}
